apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: davtro-website-monitor
  labels: { release: prometheus-stack }
spec:
  selector: { matchLabels: { app: davtro-website-app } }
  namespaceSelector: { matchNames: [ "davtro" ] }
  endpoints:
  - port: http 
    path: /metrics
    interval: 30s
